# ─────────────────────────────────────────────
# AttendEase Client — Local Environment Variables
# Copy this file to .env.local and fill in your values.
# .env.local is git-ignored by Vite automatically.
# ─────────────────────────────────────────────

# ── For LOCAL development (default, no file needed) ──
# VITE_API_URL=http://localhost:5000
# VITE_SOCKET_URL=http://localhost:5000

# ── For NGROK session ─────────────────────────
# Replace the URL below with your current ngrok HTTPS URL each session.
# Run: ngrok http 5000
# Copy the "Forwarding" URL (e.g. https://abc123.ngrok-free.app)
# Then rebuild the client: cd client && npm run build

VITE_API_URL=https://<id>.ngrok-free.app
VITE_SOCKET_URL=https://<id>.ngrok-free.app
